<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/home_page">

    <fragment
        android:id="@+id/home_page"
        android:name="com.example.tourapp.home_page"
        android:label="fragment_home_page"
        tools:layout="@layout/fragment_home_page" >
        <action
            android:id="@+id/action_home_page_to_student_login"
            app:destination="@id/student_login" />
    </fragment>
    <fragment
        android:id="@+id/student_login"
        android:name="com.example.tourapp.student_login"
        android:label="fragment_student_login"
        tools:layout="@layout/fragment_student_login" />
</navigation>